export type SidePanelTab = "home" | "tryon" | "circle" | "feed" | "wornby" | "fit";

/** Active reference photo row for the signed-in user (extension mirrors web Settings query). */
export type ReferenceStatus = "loading" | "ready" | "missing";
